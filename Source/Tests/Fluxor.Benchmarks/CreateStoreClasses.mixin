namespace {{ moxy.Class.Namespace }}
{
	public class Action1 { }

	{{ for $stateIndex in 1..50 }}
		[FeatureState]
		public class TestState{{$stateIndex}}
		{
		}

			{{ for $reducerIndex in 1..50}}
				public static class Reducers_{{$stateIndex}}_{{$reducerIndex}}
				{
						[ReducerMethod]
						public static TestState{{$stateIndex}} ReduceActionA{{$reducerIndex}}(TestState{{$stateIndex}} state, Action1 action) => state;
				}
			{{ end }}

			{{ for $effectIndex in 1..50}}
				public static class Effects_{{$stateIndex}}_{{$effectIndex}}
				{
						[EffectMethod]
						public static Task HandleActionA{{$effectIndex}}(Action1 action, IDispatcher dispatcher) => Task.CompletedTask;
				}
			{{ end }}
	{{ end }}
}