namespace {{ moxy.Class.Namespace }}
{
	public class Action1 { }

	{{ for $stateIndex in 1..100 }}
		[FeatureState]
		public class TestState{{$stateIndex}}
		{

		}

		public static class Reducers{{$stateIndex}}
		{
			{{ for $reducerIndex in 1..100}}
				[ReducerMethod]
				public static TestState{{$stateIndex}} ReduceAction{{$reducerIndex}}(TestState{{$stateIndex}} state, Action1 action) => state;
			{{ end }}
		}

		public static class Effects{{$stateIndex}}
		{
			{{ for $effectIndex in 1..100}}
				[EffectMethod]
				public static Task HandleAction{{$effectIndex}}(Action1 action, IDispatcher dispatcher) => Task.CompletedTask;
			{{ end }}
		}
	{{ end }}
}